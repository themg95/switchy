{
	"schemaVersion": 1,
	"id": "${compatId}",
	"version": "${version}",
	"name": "${compatName}",
	"description": "${compatDescription}",
	"authors": [
		"${authors}"
	],
	"contributors": [
		"${contributors}"
	],
	"contact": {
		"homepage": "${homepage}",
		"issues": "${issues}",
		"sources": "${sources}"
	},
	"license": "${license}",
	"icon": "assets/${modId}/icon.png",
	"custom": {
		"modmenu": {
			"parent": "${modId}"
		}
	},
	"mixins": [
		"${compatId}.mixins.json"
	],
	"depends": {
		"minecraft": ">=${mc}",
		"fabricloader": ">=${fl}",
		"fabric-api": ">=${fapi}",
		"${coreId}": ">=${version}",
		"${clientId}": ">=${version}"
	},
	"suggests": {
		"styled-nicknames": ">=${styledNicknames}",
		"fabrictailor": ">=${fabrictailor}",
		"fabrication": ">=${fabrication}"
	},
	"entrypoints": {
		"switchy": [
			"folk.sisby.switchy.SwitchyCompat"
		]
	}
}
